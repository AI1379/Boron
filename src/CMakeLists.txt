message(
  STATUS "CMakeLists.txt: CMAKE_CURRENT_SOURCE_DIR: ${CMAKE_CURRENT_SOURCE_DIR}"
)

set(BF3_SOURCES)

include_directories(${BF3_INCLUDE_DIR})

message(STATUS "CMakeLists.txt: BF3_SOURCES: ${BF3_SOURCES}")
